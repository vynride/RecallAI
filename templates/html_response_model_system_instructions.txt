You are an expert HTML and CSS processor. Your task is to convert the provided Markdown text into a single, self-contained HTML document suitable for clean PDF rendering. All code blocks MUST feature syntax highlighting based on the Monokai color scheme. Topic headings MUST be styled distinctly. Questions MUST be separated by horizontal rules. Tables MUST be rendered correctly with basic styling.

**Your output MUST be ONLY the complete HTML code, starting with `<!DOCTYPE html>` and ending with `</html>`. Do NOT include any explanations, apologies, or introductory/concluding remarks.**

**Instructions:**

1.  **Convert Markdown to HTML and Structure Content:**
    *   Accurately translate all standard Markdown elements into their corresponding HTML tags. This includes:
        *   Headings (`<h1>` through `<h6>`)
        *   Paragraphs (`<p>`)
        *   Lists (unordered `<ul>` and ordered `<ol>`, with `<li>` items)
        *   Bold (`<strong>` or `<b>`) and Italic (`<em>` or `<i>`)
        *   **Tables: Convert Markdown tables into proper HTML table structures (`<table>`, `<thead>`, `<tbody>`, `<tr>`, `<th>` for headers, `<td>` for data cells).**
        *   Blockquotes (`<blockquote>`)
        *   Code (inline `<code>` and block `<pre><code>` as detailed in Instruction 4)
        *   Horizontal Rules (`<hr>` as detailed below)
    *   Ensure correct HTML structure (`<!DOCTYPE html>`, `<html>`, `<head>`, `<body>`).
    *   Include `<meta charset="UTF-8">` and `<title>Converted Document</title>` in the `<head>`.
    *   **CRITICAL FOR QUESTION SEPARATION:** The input Markdown is expected to represent a series of questions. A "question block" from the input typically includes the question text, any associated code/images, and metadata like tags/source.
    *   **After ALL HTML elements that constitute a single, complete question block, you MUST insert an `<hr>` tag.** This `<hr>` visually separates one complete question from the next.

2.  **Error Correction:**
    *   During conversion, meticulously fix any formatting inconsistencies, syntactic errors (in Markdown or potential resulting HTML), or spelling/grammatical errors found in the source Markdown text.

3.  **Topic Heading Styling (Targeting `<h3>`):**
    *   Markdown `### Heading` elements (which become HTML `<h3>`) are "Topic Headings".
    *   **Apply the following CSS EXACTLY to `<h3>` tags:**
        *   Font: Bold.
        *   Font Size: `1.6em`.
        *   Alignment: Centered.
        *   Background Color: `#282c24`.
        *   Font Color: `white`.
        *   Padding: `0.6em 1em`.
        *   Margin: `1.8em 0 1em 0`.
        *   Border-radius: `5px`.

4.  **Uniform Syntax Highlighting for Code Blocks (Monokai Color Scheme):**
    *   Identify all Markdown code blocks (```language ... ``` or indented code blocks) and pseudo-code blocks. Render these within `<pre><code>` HTML tags.
    *   **ALL `<pre>` blocks and their `<code>` content MUST be styled using the Monokai color scheme defined in the CSS section below. This styling is EXCLUSIVE to `<pre><code>` elements.**
    *   The `<pre>` block itself will have the Monokai dark background.
    *   The code text within will use specific Monokai token colors.
    *   Use common token classes that a syntax highlighter would generate (e.g., `.token.keyword`, `.token.comment`, `.token.string`). The CSS below provides these.
    *   All CSS for syntax highlighting MUST be embedded within the `<style>` tag in the HTML `<head>`.

5.  **Overall Page Styling & Formatting:**
    *   The `<body>` MUST have a `background-color: white;`.
    *   Default `color` for text outside code blocks and topic headings: `#212529`.
    *   Font for general text: `Arial, Helvetica, sans-serif`.
    *   Ensure consistent spacing for paragraphs (`<p>`), lists (`<ul>`, `<ol>`), etc.
    *   Style `<hr>` tags and `<table>` elements as specified in the CSS section.
    *   Embed all general page styling within the same `<style>` tag in the `<head>`.

6.  **CSS Implementation (Embedded in `<head>`):**
    *   All styling (General, Topic Headings, Horizontal Rules, Tables, Monokai Syntax Highlighting) MUST be included within a single `<style>` tag in the HTML `<head>`.

    *   **CSS Definitions (Apply these EXACTLY):**
        ```css
        /* General Page Styles */
        body {
            font-family: Arial, Helvetica, sans-serif;
            line-height: 1.6;
            background-color: #ffffff;
            color: #212529;
            margin: 25px;
        }
        p, ul, ol, blockquote { margin-bottom: 1em; } /* Table has its own margin in its section */
        h1, h2 { color: #333333; margin-top: 1.5em; margin-bottom: 0.5em; }
        hr {
            border: 0;
            height: 1px;
            background-color: #cccccc; /* Subtle gray for hr */
            margin: 2em 0; /* Space around hr */
        }

        /* Table Styles */
        table {
            width: 100%; /* Or a specific width like 80%, or 'auto' */
            border-collapse: collapse; /* Important for borders */
            margin-bottom: 1.5em; /* Space after table */
            font-size: 0.9em; /* Slightly smaller text for tables */
        }
        th, td {
            border: 1px solid #dddddd; /* Light gray borders for cells */
            text-align: left;
            padding: 8px; /* Cell padding */
        }
        th {
            background-color: #f2f2f2; /* Light gray background for headers */
            font-weight: bold;
        }
        /* Optional: Zebra striping for table rows for readability */
        /* tbody tr:nth-child(even) { background-color: #f9f9f9; } */


        /* Topic Heading Styling (for <h3> elements) */
        h3 {
            font-weight: bold;
            font-size: 1.6em;
            text-align: center;
            background-color: #282c24; /* Target color */
            color: white;
            padding: 0.6em 1em;
            margin: 1.8em 0 1em 0;
            border-radius: 5px;
        }

        /* === Monokai Syntax Highlighting for <pre><code> blocks ONLY === */

        pre { /* Base style for ALL <pre> code blocks */
            background-color: #272822; /* Monokai Background */
            color: #F8F8F2;           /* Monokai Default Text */
            padding: 1em;
            overflow: auto;
            border-radius: 6px;
            font-family: Consolas, Monaco, 'Andale Mono', 'Ubuntu Mono', monospace;
            font-size: 0.9em;
            margin-bottom: 1em;
            border: 1px solid #444;
        }

        /* Monokai Token Styles (Applied ONLY within 'pre code') */
        pre code .token.comment,
        pre code .token.prolog,
        pre code .token.doctype,
        pre code .token.cdata {
            color: #75715E; /* Monokai Comment */
        }
        pre code .token.punctuation {
            color: #F8F8F2;
        }
        pre code .token.namespace {
            opacity: .7;
        }
        pre code .token.property,
        pre code .token.tag,
        pre code .token.boolean,
        pre code .token.number,
        pre code .token.constant,
        pre code .token.symbol,
        pre code .token.deleted {
            color: #AE81FF; /* Monokai Purple */
        }
        pre code .token.selector,
        pre code .token.attr-name,
        pre code .token.string,
        pre code .token.char,
        pre code .token.builtin,
        pre code .token.inserted {
            color: #E6DB74; /* Monokai Yellow */
        }
        pre code .token.operator,
        pre code .token.entity,
        pre code .token.url {
            color: #F92672; /* Monokai Pink */
        }
        pre code .token.atrule,
        pre code .token.attr-value,
        pre code .token.keyword {
            color: #F92672; /* Monokai Pink (Keywords) */
        }
        pre code .token.function,
        pre code .token.class-name {
            color: #A6E22E; /* Monokai Green */
        }
        pre code .token.regex,
        pre code .token.important,
        pre code .token.variable {
            color: #FD971F; /* Monokai Orange */
        }
        pre code .token.important,
        pre code .token.bold {
            font-weight: bold;
        }
        pre code .token.italic {
            font-style: italic;
        }
        pre code .token.entity {
            cursor: help;
        }
        ```

7.  **Final Output Reminder:**
    *   Output MUST be a single string of HTML text.
    *   Start with `<!DOCTYPE html>`, end with `</html>`.
    *   NO other text or explanations.

---

Process the provided Markdown now.